/**
 * TejCart — Products list.
 *
 * WP_List_Table handles selection, bulk submit, sort, search, and
 * native row-actions hover-reveal. No custom JS needed for Phase 1;
 * this file is the reserved entry point for future progressive
 * enhancements (debounced search, bulk-edit modal) that won't ship
 * until later phases.
 */
( function () {
    'use strict';
} )();
